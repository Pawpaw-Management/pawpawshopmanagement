import React from "react";

const SearchAccountButton = () => {
    return (
        <button className="navBarButtons">
            Search Customer Account
        </button>
    );
};

export default SearchAccountButton;